import { HighlightCsDirective } from './highlight-cs.directive';

describe('HighlightCsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCsDirective();
    expect(directive).toBeTruthy();
  });
});
